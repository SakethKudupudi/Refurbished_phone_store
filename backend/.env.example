# Azure SQL Database Configuration
# Copy this file to `.env` and fill in your values (do NOT commit real secrets)

# Azure SQL Server (without .database.windows.net suffix)
AZURE_SQL_SERVER=mobileparts-server.database.windows.net

# Azure SQL Database Name
AZURE_SQL_DATABASE=db-mobileparts

# Azure SQL Admin Username
AZURE_SQL_USERNAME=sql_admin

# Azure SQL Admin Password (set locally in your .env)
AZURE_SQL_PASSWORD=YOUR_AZURE_SQL_PASSWORD

# Spring Profile (use 'sqlserver' for Azure SQL, 'supabase' for Supabase)
SPRING_PROFILES_ACTIVE=sqlserver

# Supabase/Postgres example placeholders (if using supabase profile)
# DB URL format (use in application-supabase.properties or env):
# postgresql://<user>:<password>@db.<project>.supabase.co:5432/postgres
SUPABASE_DB_USERNAME=postgres
SUPABASE_DB_PASSWORD=YOUR_SUPABASE_PASSWORD
SUPABASE_DB_HOST=db.uqiawhrnbjtikdxclukn.supabase.co
SUPABASE_DB_NAME=postgres
