# Azure SQL Database Configuration
# Copy this file to .env and fill in your values

# Azure SQL Server (without .database.windows.net suffix)
AZURE_SQL_SERVER=mobileparts-server.database.windows.net

# Azure SQL Database Name
AZURE_SQL_DATABASE=db-mobileparts

# Azure SQL Admin Username
AZURE_SQL_USERNAME=sql_admin

# Azure SQL Admin Password
# Check DNS resolution
nslookup db.uqiawhrnbjtikdxclukn.supabase.co

# or dig if installed
dig +short db.uqiawhrnbjtikdxclukn.supabase.co

# Test simple HTTPS reachability to Supabase API host
curl -I https://uqiawhrnbjtikdxclukn.supabase.co || echo "curl failed"# IMPORTANT: Do NOT commit real passwords to source control. Copy this file to `.env` and set the real password there.
AZURE_SQL_PASSWORD=YOUR_AZURE_SQL_PASSWORD

# Spring Profile (use 'sqlserver' for Azure SQL)
SPRING_PROFILES_ACTIVE=sqlserver
